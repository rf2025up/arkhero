# ArkHero 积分操作面板改造计划

## 一、技术架构总览

| 层级 | 技术 | 说明 |
|------|------|------|
| **前端** | React 19 + TypeScript + Vite + TailwindCSS | SPA，react-router-dom v7 |
| **后端** | Express.js + TypeScript | REST API + JWT认证 + Socket.IO |
| **数据库** | PostgreSQL | Prisma ORM v5 |
| **实时通信** | Socket.IO v4 | 学校房间广播积分变化 |
| **部署** | Sealos (Kubernetes) | 容器化部署 |

### 核心文件清单（本次改造涉及）

| 文件 | 路径 | 作用 |
|------|------|------|
| **schema.prisma** | `server/prisma/schema.prisma` | 数据库模型定义 |
| **student.service.ts** | `server/src/services/student.service.ts` | 积分操作业务逻辑（addScore） |
| **student.routes.ts** | `server/src/routes/student.routes.ts` | API路由（/score、/last-score） |
| **ActionSheet.tsx** | `client/src/components/ActionSheet.tsx` | 长按弹出的积分操作面板 |
| **Home.tsx** | `client/src/pages/Home.tsx` | 长按交互、调用API |
| **api.service.ts** | `client/src/services/api.service.ts` | Axios API客户端 |

---

## 二、现状分析（问题诊断）

### 2.1 当前积分操作流程
```
老师长按学生头像(600ms) → 触发振动+打开ActionSheet
→ 输入积分/经验值/原因
→ 点击"确认加分"
→ POST /api/students/score
→ 后端addScore()：更新students.points + 创建task_records记录 + Socket广播
```

### 2.2 存在的问题

| # | 问题 | 影响 |
|---|------|------|
| 1 | **只显示1条上次操作记录** | 老师无法看到历史操作趋势 |
| 2 | **减分时无理由选择** | 无法区分"减分惩罚"和"积分兑换"，记录混淆 |
| 3 | **task_records缺少reason_type字段** | 数据库无法区分减分类型，无法做兑换统计 |
| 4 | **确认按钮固定显示"确认加分"** | 减分时按钮文字不匹配，操作意图不清 |
| 5 | **addScore用updatedBy存的是username** | 可以追溯到操作员，但字段语义不明确 |

### 2.3 数据库现状

**task_records 表**（当前积分变化存储方式）：
```prisma
model task_records {
  type          TaskType          // SPECIAL(加分) / CHALLENGE(减分)
  title         String            // "手动加分" / "手动扣分" / "经验调整"
  content       Json?             // { score, exp, metadata: { updatedBy, ... } }
  expAwarded    Int               // 经验值
  status        TaskStatus        // COMPLETED
  task_category TaskCategory      // SPECIAL / CHALLENGE 等
}
```

**关键问题**：
- `content.metadata.updatedBy` 存的是username（string），不是teacher id
- 没有专门的 `reason_type` 字段区分"减分"和"积分兑换"
- 查询历史记录靠 title LIKE '%手动%' 匹配，不够精确

---

## 三、改造计划（5步）

### 步骤1：数据库层 —— 新增字段 + 迁移

**改动文件**：`server/prisma/schema.prisma`

在 `task_records` 模型新增 `reason_type` 字段：

```prisma
model task_records {
  // ... 现有字段 ...
  reason_type   String?           // 🆕 减分类型：DEDUCT(减分惩罚) / EXCHANGE(积分兑换)
}
```

**执行迁移**：
```bash
cd server
npx prisma migrate dev --name add_reason_type_to_task_records
```

> 💡 使用可选字段 `String?`，不破坏现有数据。旧记录 reason_type 为 null。

---

### 步骤2：后端API层 —— 三项改造

**改动文件**：`server/src/services/student.service.ts` + `server/src/routes/student.routes.ts`

#### 2.1 addScore() 支持传入 reason_type

```typescript
// student.service.ts - addScore方法
// content.metadata 新增 reasonType 字段
content: {
  score: points,
  exp,
  metadata: {
    ...metadata,
    updatedBy,           // 操作员username
    reasonType: metadata?.reasonType || null,  // 🆕 DEDUCT / EXCHANGE
    previousLevel: ...,
    newLevel: ...
  }
}
```

#### 2.2 新增 API：获取最近7条积分操作记录

**路由**：`GET /api/students/:id/score-history?limit=7`

```typescript
// student.routes.ts - 新增路由
this.router.get('/:id/score-history', this.getScoreHistory.bind(this));
```

```typescript
// 实现：查询 task_records，返回最近7条手动积分操作
// 返回格式：
[
  {
    id: string,
    points: number,        // 积分变化量
    exp: number,           // 经验变化量
    reason: string,        // 原因
    reasonType: string,    // 🆕 DEDUCT / EXCHANGE / null
    operatorName: string,  // 操作员
    operatedAt: string     // 操作时间 ISO格式
  }
]
```

#### 2.3 修改 POST /api/students/score 接收 reasonType

```typescript
// student.routes.ts - addScore方法
// 从 req.body.metadata.reasonType 传入
const data: AddScoreRequest = {
  ...req.body,
  schoolId: req.schoolId!,
  metadata: {
    ...req.body.metadata,
    reasonType: req.body.metadata?.reasonType || null  // 🆕
  }
};
```

---

### 步骤3：前端交互层 —— ActionSheet 全面改造

**改动文件**：`client/src/components/ActionSheet.tsx` + `client/src/pages/Home.tsx`

#### 3.1 显示最近7条操作记录（替代原来只显示1条）

```
┌─────────────────────────────────────┐
│  📋 最近操作记录                     │
├─────────────────────────────────────┤
│  5-18  张老师  -10积分  [积分兑换]    │
│  5-16  李老师  +5积分   [手动加分]    │
│  5-15  张老师  -3积分   [减分]       │
│  5-14  张老师  +10积分  [手动加分]    │
│  5-12  李老师  -5积分   [积分兑换]    │
│  5-10  张老师  +3积分   [手动加分]    │
│  5-08  张老师  -2积分   [减分]       │
└─────────────────────────────────────┘
```

- 调用新API `GET /students/:id/score-history?limit=7`
- 每条记录显示：时间、操作员、积分变化、理由类型标签
- 积分兑换高亮显示（橙色标签），减分灰色标签

#### 3.2 减分时弹出理由选择弹窗

当输入积分为负数时，确认按钮点击后弹出二次确认：

```
┌─────────────────────────────────────┐
│         请选择减分原因               │
│                                     │
│  ┌───────────────┐ ┌──────────────┐ │
│  │  📉 减分惩罚   │ │  🎁 积分兑换 │ │
│  └───────────────┘ └──────────────┘ │
│                                     │
│          [取消]    [确认]            │
└─────────────────────────────────────┘
```

- 必须选择一个理由才能提交
- 选"积分兑换"：reasonType = 'EXCHANGE'
- 选"减分惩罚"：reasonType = 'DEDUCT'
- 加分时不需要此步骤

#### 3.3 按钮文字动态显示

- 积分 > 0 时：显示"确认加分"（现有）
- 积分 < 0 时：显示"确认减分"
- 积分 = 0 时：显示"确认调整"

---

### 步骤4：数据校验 —— 确保操作写入数据库

**改动文件**：`server/src/services/student.service.ts`

#### 4.1 addScore 事务完整性检查

```typescript
async addScore(data: AddScoreRequest, updatedBy: string): Promise<any[]> {
  // ✅ 已使用 prisma.$transaction，确保原子性
  // 新增：记录操作日志，确认事务提交
  console.log(`[SCORE] ✅ Transaction committed: ${studentIds.length} students, points=${points}, reasonType=${data.metadata?.reasonType}`);
  
  // 新增：验证数据确实写入
  const verifyStudent = await this.prisma.students.findFirst({
    where: { id: studentIds[0] },
    select: { points: true, exp: true }
  });
  console.log(`[SCORE] ✅ Verification: student ${studentIds[0]} points=${verifyStudent?.points}`);
}
```

#### 4.2 Socket广播确认

```typescript
// 广播后增加确认日志
this.broadcastToSchool(schoolId, broadcastData);
console.log(`[SCORE] 📡 Broadcast sent for score update: ${studentIds}`);
```

---

### 步骤5：测试验证清单

| # | 测试项 | 预期结果 |
|---|--------|----------|
| 1 | 长按学生 → 加分5积分 | 数据库students.points+5，task_records新增1条 |
| 2 | 长按学生 → 减分5积分 → 选择"减分惩罚" | points-5，task_records.reason_type='DEDUCT' |
| 3 | 长按学生 → 减分10积分 → 选择"积分兑换" | points-10，task_records.reason_type='EXCHANGE' |
| 4 | 打开操作面板 → 查看历史记录 | 显示最近7条，包含时间/操作员/理由 |
| 5 | 加分不弹理由选择 | 直接提交，无二次弹窗 |
| 6 | 减分不选理由 → 点确认 | 提示"请选择减分原因" |
| 7 | 多次操作后检查数据库 | 所记录的points值与数据库一致 |
| 8 | Socket广播 | 大屏实时更新学生积分 |

---

## 四、改动文件总览

| 文件 | 改动类型 | 改动量 | 风险等级 |
|------|----------|--------|----------|
| `schema.prisma` | 新增字段 | +1行 | 🟢 低（可选字段） |
| `student.service.ts` | 修改+新增 | ~50行 | 🟡 中（核心业务） |
| `student.routes.ts` | 新增路由 | ~60行 | 🟢 低 |
| `ActionSheet.tsx` | 重构 | ~120行 | 🟡 中（UI交互） |
| `Home.tsx` | 修改 | ~30行 | 🟢 低 |
| **数据库迁移** | Prisma migrate | 自动生成 | 🟡 中（需备份数据库） |

---

## 五、执行顺序

1. ✅ **数据库迁移**（先加字段，不破坏现有数据）
2. ✅ **后端API**（新增路由 + 修改addScore）
3. ✅ **前端交互**（ActionSheet改造 + Home.tsx适配）
4. ✅ **数据校验**（事务验证 + 日志增强）
5. ✅ **测试验证**（手动测试以上8个场景）

预计改动代码量：~260行（不含迁移文件）
